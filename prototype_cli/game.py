import termcolor
def create_board():
	board = []
	board.append([''].extend(range(0,11))
	return board
    """
	for row in range(11):
		row_positions = []
		for col in range(11):
			row_positions.append
	"""

if __name__ == '__main__':
	board = create_board()
